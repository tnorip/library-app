// Day 4 ワーク: 図書登録フォームコンポーネントを実装する
//
// ゴール:
//   - タイトル・著者を入力できるフォームを作る
//   - POST /books を呼び出して新しい図書を登録する
//   - 登録成功後は BookList を再取得する
//
// ヒント: api.post<Book>('/books', { title, author }) を使う (src/api/client.ts を参照)

export default function BookForm() {
  return <p>（BookForm を実装してください）</p>
}
